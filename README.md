# Personal Site

Next.js + TypeScript + Tailwind, statically exported and deployed to GitHub Pages.

## Quick start

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Build

```bash
npm run build   # outputs to out/
npx serve out   # preview the static export (npm run start does not work)
```
